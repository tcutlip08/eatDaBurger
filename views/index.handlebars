<h2>EAT DA BURGER!!</h2>

<ul>
  {{#each burgers}}
  <li>
    {{burger_name}}
    {{#unless devoured}} {{> burgers/burger-block devoured=false}} {{/unless}}
    <button class="delete-burger" data-id="{{this.id}}">Delete</button>
  </li>
  {{/each}}
</ul>