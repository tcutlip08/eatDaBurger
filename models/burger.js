var orm = require("../config/orm");

// No Params
orm.selectAll();

// only needs name of new burger
// orm.insertOne("Name of Burger");

// Name of new burger and id of what its replacing
// orm.updateOne("Tits", 1);